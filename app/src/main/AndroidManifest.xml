<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chawki.carbon">

    <application
        android:allowBackup="true"
        android:icon="@drawable/rsz_1image1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/rsz_1image1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu2Activity" />
        <activity android:name=".Menu3Activity" />
        <activity android:name=".product.ProductActivity" />
        <activity android:name=".product.Product2Activity" />
        <activity android:name=".BasketActivity"></activity>
    </application>

</manifest>